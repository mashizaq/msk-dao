import React from 'react';\nimport Link from 'next/link';\nimport { useRouter } from 'next/router';\nimport { motion } from 'framer-motion';\n\nconst navItems = [\n  { label: 'Dashboard', path: '/dashboard', icon: '📊' },\n  { label: 'Governance', path: '/governance', icon: '🗳️' },\n  { label: 'Treasury', path: '/treasury', icon: '💰' },\n  { label: 'Members', path: '/members', icon: '👥' },\n  { label: 'Projects', path: '/projects', icon: '🚀' },\n];\n\nexport default function Navigation() {\n  const router = useRouter();\n\n  return (\n    <nav className=\"bg-slate-900 border-b border-slate-700 sticky top-0 z-50\">\n      <div className=\"max-w-7xl mx-auto px-6 py-4\">\n        <div className=\"flex items-center justify-between\">\n          {/* Logo */}\n          <Link href=\"/\">\n            <motion.a\n              whileHover={{ scale: 1.05 }}\n              className=\"flex items-center gap-2 cursor-pointer\"\n            >\n              <span className=\"text-2xl\">🚀</span>\n              <span className=\"text-xl font-bold text-white\">MSK DAO</span>\n            </motion.a>\n          </Link>\n\n          {/* Nav Items */}\n          <div className=\"hidden md:flex items-center gap-1\">\n            {navItems.map((item) => (\n              <Link key={item.path} href={item.path}>\n                <motion.a\n                  whileHover={{ scale: 1.05 }}\n                  className={`px-4 py-2 rounded-lg transition-colors flex items-center gap-2 cursor-pointer ${\n                    router.pathname === item.path\n                      ? 'bg-blue-600 text-white'\n                      : 'text-slate-300 hover:bg-slate-700'\n                  }`}\n                >\n                  <span>{item.icon}</span>\n                  {item.label}\n                </motion.a>\n              </Link>\n            ))}\n          </div>\n\n          {/* Connect Wallet Button */}\n          <motion.button\n            whileHover={{ scale: 1.05 }}\n            whileTap={{ scale: 0.95 }}\n            className=\"px-6 py-2 bg-blue-600 hover:bg-blue-700 text-white rounded-lg font-semibold transition-colors\"\n          >\n            Connect Wallet\n          </motion.button>\n        </div>\n\n        {/* Mobile Menu */}\n        <div className=\"md:hidden flex items-center gap-2 mt-4 overflow-x-auto pb-2\">\n          {navItems.map((item) => (\n            <Link key={item.path} href={item.path}>\n              <motion.a\n                whileHover={{ scale: 1.05 }}\n                className={`px-3 py-1 rounded text-sm whitespace-nowrap transition-colors cursor-pointer ${\n                  router.pathname === item.path\n                    ? 'bg-blue-600 text-white'\n                    : 'bg-slate-700 text-slate-300'\n                }`}\n              >\n                {item.icon} {item.label}\n              </motion.a>\n            </Link>\n          ))}\n        </div>\n      </div>\n    </nav>\n  );\n}\n